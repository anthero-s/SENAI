print ("olá, esse programa irá receber a base e a altura ded um triangulo e retornar a area.")

base = float(input("digite a base"))
altura = float(input("digite a altura"))
def area (base):
    area = base * altura % 2
print (f"a area do triangulo {area}" )

